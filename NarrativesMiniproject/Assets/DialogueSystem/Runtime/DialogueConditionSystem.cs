using UnityEngine;
using DialogueSystem;
public static class DialogueConditionSystem
{
    public static bool Check(Condition c)
    {
        switch (c.id)
        {
            
        }

        return false;
    }
}
